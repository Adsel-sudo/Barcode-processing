# Barcode Processing - minimal runtime dependencies (v1)
# Mainline: PDF native text + coordinate clustering (no OCR by default)

pymupdf>=1.24,<2.0
Pillow>=10.0,<12.0

# Optional: enable when you need Excel export (CSV/JSON can use stdlib)
# pandas>=2.2,<3.0
# openpyxl>=3.1,<4.0
